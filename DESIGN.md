# Aurum · 日月硬币

视觉：深墨绿底上的温暖金属浮雕，以日曜与月相作为同一枚硬币的双面，保留齿边、拉丝与雕刻层次。

内容：一个可独立嵌入的模拟器为主，三个透明背景 SVG 作为可下载素材，简短中文接入说明。

交互：点击抛掷产生随机正反结果；正反面按钮平滑翻面；抛物线位移、金属侧边投影和落地回稳共同构成动画。减少动态效果偏好下直接显示结果。

实现：所有产物由相同 SVG 定义与投影函数生成；动画 SVG 用 SMIL 自循环，组件用 requestAnimationFrame 按相同规则驱动。静态端点都归一到单位矩阵。无网络资源、无运行时依赖。

验收：三份 SVG XML 有效；静态图与动画停留帧做浏览器像素对比；组件正反互转、随机抛掷、防重复点击、多实例、键盘和窄屏检查；浏览器无运行错误。
